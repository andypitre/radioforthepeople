node_modules
dist
build
.output
.vinxi
.tanstack
.nitro

*.log
.DS_Store

.env
.env.local
.env.*.local

# Deploy: the filled YAML and production env both contain secrets
deploy/nexlayer.yaml
deploy/.env.production

server/recordings/*
!server/recordings/.gitkeep

pg-data/

# DB dumps contain user data — keep local-only
backups/
