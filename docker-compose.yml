services:
  postgres:
    image: postgres:16
    container_name: rftp-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: rftp
      POSTGRES_PASSWORD: rftp
      POSTGRES_DB: radioforthepeople
    ports:
      - "5432:5432"
    volumes:
      - ./pg-data:/var/lib/postgresql/data
